import { View } from "react-native";
import { Text } from "react-native";

const Home = () => {
  return (
    <View className="min-h-dvh w-dvw flex flex-col items-center justify-center">
      <Text className="text-2xl">Home</Text>
    </View>
  );
}

export default Home